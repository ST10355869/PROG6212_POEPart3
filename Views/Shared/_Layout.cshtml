<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - ST10355869_PROG6212_Part2</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ST10355869_PROG6212_Part2.styles.css" asp-append-version="true" />
    <style>
        /* Header Styling */
        header {
            color: whitesmoke;
            font-size: 18px;
        }

        .navbar {
            background-color: #007bff !important; /* Header color */
        }

        .navbar-brand, .nav-link {
            color: whitesmoke !important;
        }

        /* Page Background Styling */
        body {
            background-color: #f8f9fa; /* Off-white background */
        }

        /* Footer Styling */
        footer {
            background-color: #151515;
        }

        .footer a {
            color: whitesmoke;
        }
    </style>
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-light bg-light">
            <a class="navbar-brand" href="/">MyApp</a>
            <div class="collapse navbar-collapse">
                <ul class="navbar-nav mr-auto">
                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        if (User.IsInRole("User"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Lecturer/SubmitClaim">Submit Claim</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/Lecturer/ClaimStatus">Claims List</a>
                            </li>
                        }
                        if (User.IsInRole("Admin"))
                        {
                            <li class="nav-item">
                                <a class="nav-link" href="/Manager/Admin">Admin Page</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/Lecturer/SubmitClaim">Submit Claim</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="/Manager/EditLecturer/1">Edit Lecturer</a>
                            </li>
                        }
                    }
                </ul>
                <ul class="navbar-nav">
                    @if (User.Identity?.IsAuthenticated ?? false)
                    {
                        <li class="nav-item">
                            <form asp-area="" asp-controller="Account" asp-action="Logout" method="post" class="form-inline">
                                <button type="submit" class="btn btn-link nav-link" style="cursor:pointer;">Logout</button>
                            </form>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-area="" asp-controller="Account" asp-action="Login">Login</a>
                        </li>
                    }
                </ul>
            </div>
        </nav>
    </header>

    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2023 - My ASP.NET Application - <a href="/Privacy">Privacy</a>
        </div>
    </footer>

    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js"></script>
    @RenderSection("Scripts", required: false)
</body>
</html>
